[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --tb=short
markers =
    slow: exercises the Streamlit interface end to end (solves the room for real)
filterwarnings =
    ignore::DeprecationWarning
