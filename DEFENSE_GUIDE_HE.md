# מדריך הגנה — Project R-5

מדריך זה נכתב על בסיס הקוד בפועל. כל מספר בו נמדד מהמימוש, לא מהערכה.

---

## 1. מושגי יסוד

| מושג | פירוש | איפה זה בקוד |
|---|---|---|
| **Agent** (סוכן) | הלומד. הוא בוחר פעולה בכל צעד ומעדכן את מה שלמד. | `game/algorithms/` |
| **Environment** (סביבה) | העולם. מקבל פעולה ומחזיר מצב חדש, תגמול, והאם האפיזודה נגמרה. | `game/grid.py`, `drone.py`, `warehouse.py` |
| **State** (מצב) | כל מה שצריך לדעת כדי להחליט. חייב לקיים את תכונת Markov. | `env.reset()` / `env.step()` |
| **Action** (פעולה) | מה שהסוכן יכול לעשות. | `env.actions()` |
| **Reward** (תגמול) | מספר יחיד שהסביבה מחזירה על כל צעד. | `room["rewards"]` |
| **תגמול חיובי** | פרס. למשל הגעה ליציאה: `+38` בחדר 2. | |
| **תגמול שלילי** | עלות או ענישה. למשל `-1` על כל צעד. | |
| **Immediate reward** | התגמול של צעד בודד, `r_t`. | |
| **Episode Return** | **סכום** כל התגמולים באפיזודה: `G = r_1 + r_2 + ... + r_T`. זה מה שהגרף "Episode Return" מציג — לא התגמול האחרון. | `session.py`, `episode["reward"] += reward` |
| **Episode** (אפיזודה) | ניסיון שלם: מהתחלה עד מצב סופי או עד תום הזמן. | |
| **Terminal state** | מצב שמסיים את האפיזודה. למשל הפיר בחדר 2. | `env.is_terminal()` |
| **Timeout / truncation** | האפיזודה נקטעה כי נגמרו הצעדים, לא כי הגיעה למצב סופי. הבחנה חשובה: timeout אינו כישלון של המשימה. | `outcome == "timeout"` |
| **Policy** (מדיניות) | הכלל שלפיו נבחרת פעולה מכל מצב: `π(s) → a`. | `algorithm.act()` |
| **Value function** `V(s)` | התוחלת של ה-Return מהמצב `s` והלאה. | חדר 1 |
| **Q function** `Q(s,a)` | התוחלת של ה-Return מהמצב `s` אחרי הפעולה `a`. | חדרים 2–5 |
| **Exploration vs exploitation** | לנסות משהו חדש כדי ללמוד, מול לנצל את מה שכבר ידוע. | |
| **ε-greedy** | בהסתברות `ε` פעולה מקרית, אחרת הפעולה הטובה ביותר הידועה. | `algorithms/base.py` |
| **α** (learning rate) | כמה מהאומדן החדש לשמור בכל עדכון. גדול = לימוד מהיר ורועש; קטן = איטי ויציב. | |
| **γ** (discount) | כמה שווה תגמול עתידי מול תגמול מיידי. קרוב ל-1 = אופק ארוך. | |
| **ε** (epsilon) | שיעור החקירה הנוכחי. | |
| **ε decay** | מכפיל שמוריד את `ε` אחרי כל אפיזודה. | |
| **ε minimum** | הרצפה שמתחתיה `ε` לא יורד, כדי שהחקירה לא תיפסק לגמרי. | |
| **TD target** | היעד שאליו מושכים את האומדן. ב-Q-Learning: `r + γ·max_a' Q(s',a')`. ב-SARSA: `r + γ·Q(s',a')`. | |
| **TD error** `δ` | ההפרש בין היעד לאומדן הנוכחי: `δ = TD target − Q(s,a)`. | |
| **Convergence** | התייצבות. בחדר 1 נמדדת כ-`delta` בין סריקות; בחדרים 2–5 כ-`mean(|δ|)`. | |
| **Moving average** | ממוצע נע. כאן: ממוצע אחורי של 20 אפיזודות. | `charts.js` |
| **Seed** | זרע האקראיות. אותו seed ואותם פרמטרים = אותה ריצה בדיוק. | |
| **Training / validation / test** | שלוש קבוצות **נפרדות** של מפות בחדר 5: אחת ללמידה, אחת לבדיקה תקופתית, ואחת שלא אומנו עליה כלל. | `warehouse.py` pools |

---

## 2. הבחנה חשובה — אל תגידו "max state"

הביטוי הזה עמום. יש ארבעה דברים שונים:

| מה | חדר 1 | חדר 2 | חדר 3 | חדרים 4–5 |
|---|---|---|---|---|
| **גודל מרחב המצבים** (כמה מצבים קיימים) | 810 | 4,000 | 23,808 | אינסופי — מצב רציף |
| **מספר הצעדים המקסימלי באפיזודה** | אין אפיזודות | 400 | 400 | 1,800 / 300 |
| **ערך Q מקסימלי** (המספר הגדול בטבלה) | לא רלוונטי — `V` ולא `Q` | תלוי בריצה | תלוי בריצה | אין טבלה |
| **המצב האחרון באפיזודה** | אין | הפיר / היציאה | היציאה / נתפס | נחיתה / התנגשות |

המספרים 810 / 4,000 / 23,808 נמדדו בקוד באמצעות `len(list(env.all_states()))`.

---

## 3. טבלת חדרים

| | חדר 1 | חדר 2 | חדר 3 | חדר 4 | חדר 5 |
|---|---|---|---|---|---|
| **שם** | Laser Security Chamber | Broken Bridge Sector | Reactor Control Chamber | Drone Wind Tunnel | Adaptive Storage Facility |
| **אלגוריתם** | Value Iteration | SARSA | Q-Learning | Semi-gradient SARSA + Tile Coding | Semi-gradient Q-Learning |
| **המודל ידוע?** | **כן** | לא | לא | לא | לא |
| **מרחב מצבים** | 810 | 4,000 | 23,808 | רציף | רציף |
| **פעולות** | 4 | 4 | **5** (כולל WAIT) | 5 | 5 |
| **max steps** | — | 400 | 400 | 1,800 | 300 |
| **אפיזודות ברירת מחדל** | — | 1,500 | 4,000 | 1,200 | 4,000 |
| **קבצים** | `grid.py`, `algorithms/value_iteration.py` | `grid.py`, `algorithms/sarsa.py` | `grid.py`, `reactor.py`, `algorithms/q_learning.py` | `drone.py`, `algorithms/semi_gradient_sarsa.py`, `tile_coding.py` | `warehouse.py`, `algorithms/semi_gradient_q.py`, `linear.py` |

---

## 4. ה-states בפועל

### חדר 1 — 810 מצבים

```
(row, col, battery_collected, last_direction, collapsed_mask)
```

* `row, col` — התא בלוח 10×10.
* `battery_collected` — האם הסוללה כבר נאספה. נחוץ כי אפשר לאסוף אותה **פעם אחת**; בלי זה המודל חושב שהיא משתלמת שוב ושוב.
* `last_direction` — הכיוון הקודם. נחוץ בגלל השמן (`o`), שממשיך את התנועה הקודמת.
* `collapsed_mask` — אין קרשים בחדר הזה, ולכן הממד מתכנס לערך אחד ולא מגדיל את המרחב.

### חדר 2 — 4,000 מצבים

אותו מבנה בסיסי. כאן `collapsed_mask` **כן** פעיל: הוא זוכר אילו קרשים כבר קרסו.

הקריסה מוגרלת **פעם אחת לחצייה**, בצעד שמעביר את הסוכן מקרקע מוצקה אל הקרשים:

```
P(חצייה נכשלת) = collapse_chance
```

נמדד על 2,000 ניסיונות לכל הגדרה: `p=0.10 → 0.101`, `p=0.25 → 0.253`, `p=0.50 → 0.524`.
זה **לא** `1-(1-p)^4` — ארבעת הקרשים לא מגרילים כל אחד בנפרד.

### חדר 3 — 23,808 מצבים

```
(row, col, unused_battery, unused_direction, unused_collapse_mask,
 generator_stage, guard_index, keys_mask)
```

* `generator_stage` — כמה גנרטורים כבר הועלו, **בסדר** (0–3).
* `guard_index` — היכן שומר הביטחון נמצא במסלול הסיור שלו.
* `keys_mask` — אילו מפתחות כבר בידיים.
* שלושת הממדים האמצעיים אינם בשימוש בחדר זה ומתכנסים לערך אחד.

**מצב הדלתות נגזר מ-`guard_index`** ולא משעון עצמאי. זה מה ששומר על תכונת Markov: מספר אחד מתאר גם את השומר וגם את הדלתות, ולכן אין מידע חבוי.

### חדר 4 — מצב רציף

```
(x, y, vx, vy)
```

* `x, y` — מיקום במטרים בעולם של 10×10 מטר.
* `vx, vy` — רכיבי מהירות במטר לשנייה, כל אחד חסום לטווח `[-1, 1]`.
* `dt = 0.02` שניות.
* **אין** מהירות זוויתית ב-state. **אין** זווית ב-state.
* הזווית שמצוירת על המסך היא `atan2(vy, vx)` — ערך **תצוגה** בלבד, שמחושב מהמהירות. הלומד לא רואה אותה.
* הפעולות **בדידות**: Hold, Up, Down, Left, Right. כל פעולה מפעילה **תאוצה** — היא לא מעבירה את הרחפן לתא הבא. הדחף משנה את המהירות, הגרר מדעיך אותה, והמיקום נגרר אחרי המהירות החדשה.
* מרחב המצבים אינסופי ולכן **אין טבלת Q**. Tile Coding פורש את המרחב ב-8 רשתות חופפות ולומד משקל לכל אריח.

### חדר 5 — מצב פנימי מול תצפית

**המצב של העולם:**

```
(x, y, vx, vy, stage, phase)
```

* `stage` — 0 לפני הגעה למסוף, 1 אחריו. אותו מיקום אומר משהו אחר בכל שלב.
* `phase` — מספר שלם חסום שממנו המיקום של **כל** מכשול נע הוא פונקציה טהורה. זה מה ששומר על Markov בלי לאחסן כל רחפן בנפרד.

**התצפית של הסוכן — 14 מספרים, כולם ב-`[0,1]`** (מתוך `WarehouseWorld._observe`, לפי הסדר בקוד):

| # | מה | נרמול |
|---|---|---|
| 1 | כיוון אל המטרה, ציר X | `0.5 + bearing_x/2` |
| 2 | כיוון אל המטרה, ציר Y | `0.5 + bearing_y/2` |
| 3 | קרן החישה המרכזית (שוב, יחד עם הכיוון) | `rays["centre"]` |
| 4 | קרן שמאל | `rays["left"]` |
| 5 | קרן מרכז | `rays["centre"]` |
| 6 | קרן ימין | `rays["right"]` |
| 7 | מיקום במחסן, ציר X | `x / width` |
| 8 | מיקום במחסן, ציר Y | `y / height` |
| 9 | מהירות `vx` | `(vx + limit) / (2·limit)` |
| 10 | מהירות `vy` | `(vy + limit) / (2·limit)` |
| 11 | כמה מהמהירות מכוונת אל המטרה | `0.5 + align/2` |
| 12 | מרחק שנותר אל המטרה | `distance / span` |
| 13 | מרחק אל הרחפן הנע הקרוב | `distance / sensor_range`, ו-`1.0` אם אין אחד בטווח |
| 14 | כמה מהר הוא מתקרב | `0.5 + towards/(4·limit)` |

שימו לב: רכיב 3 ורכיב 5 הם **אותה** קרן מרכזית. זה מכוון — היא מופיעה פעם אחת בקבוצה של כיוון המטרה ופעם בקבוצה של שלוש הקרנות, כי הצירוף "המטרה שם והדרך לשם פנויה" הוא מה שחשוב.

**ההבדל בין world state ל-observation:** הסביבה יודעת איפה כל מכשול נמצא; התצפית מכילה שלוש קרנות מוגבלות-טווח וקריאה אחת על המכשול הקרוב. שני מחסנים שונים יכולים לייצר אותם 14 מספרים, ומכשול מאחורי מדף פשוט נעדר מהם. לכן **החדר Markov והתצפית לא** — וזו בדיוק הסיבה שמדיניות שנלמדת כאן חייבת להכליל ולא לשנן.

**Sensor range וכלל centre-to-centre:** ברירת המחדל 3.0 מטר. מכשול נראה כאשר המרחק **בין המרכזים** קטן מהטווח — הרדיוס לא מוחסר.

**Layout pools:** 120 מפות אימון, 10 ולידציה, 20 מפות מבחן שלא אומנו עליהן. שלוש הקבוצות **זרות** זו לזו, והפרדה זו נאכפת בבנייה.

---

## 5. max steps בפועל

| חדר | מגבלה | זמן מדומה |
|---|---|---|
| **1** | אין אפיזודות. Value Iteration רץ ב-**sweeps** עד ש-`delta < theta`. | — |
| **2** | 400 צעדים, מ-`SESSION["max_steps_per_episode"]` | לצעד בלוח אין משך |
| **3** | 400 צעדים, מאותו כלל | לצעד בלוח אין משך |
| **4** | 1,800 **physics steps** | `1800 × 0.02 = 36` שניות |
| **5** | 300 **agent decisions** | `300 × 10 × 0.02 = 60` שניות |

החישוב לחדר 5, במלואו:

```
decision = action_repeat × dt = 10 × 0.02 = 0.2 שניות
episode  = max_steps × decision = 300 × 0.2 = 60 שניות
```

---

## 6. מספר האפיזודות והצדקתו

| חדר | אפיזודות | הצדקה |
|---|---|---|
| **1** | — | אין אפיזודות. תנאי העצירה הוא `delta < theta`, לא מספר. |
| **2** | 1,500 | המשימה קטנה ומרחב המצבים 4,000. SARSA צריך מספיק ניסיונות כדי לחוות גם את המסלול הבטוח וגם את סיכון הגשר. 1,500 הוא איזון בין התייצבות לזמן ריצה. |
| **3** | 4,000 | תגמול מושהה: שלושה מפתחות ושלושה גנרטורים **בסדר**, ועוד שומר, דלתות מתחלפות ופעולת WAIT. מרחב המצבים 23,808 — פי שש מחדר 2 — ולכן נדרשות יותר אפיזודות. |
| **4** | 1,200 | כל אפיזודה יכולה להיות ארוכה מאוד — עד 1,800 צעדי פיזיקה. Tile Coding מכליל בין מצבים סמוכים, ולכן לא נדרש כיסוי צפוף כמו בטבלה. 1,200 הוא איזון בין כיסוי, אורך אפיזודה וזמן אימון. |
| **5** | 4,000 | ה-layout משתנה בכל אפיזודה, יש 120 מפות אימון, מכשולים דינמיים ומשימה בשני שלבים. צריך ללמוד **הכללה** ולא מסלול יחיד, וזה דורש לראות הרבה מפות. |

---

## 7. הסבר הגרפים

| גרף | ציר X | ציר Y | שיפור נראה כך | מתי לחשוד |
|---|---|---|---|---|
| **Episode Return** | אפיזודה | סכום התגמולים `G` | עולה ואז מתיישר | אם יורד בהתמדה — α גדול מדי או γ לא מתאים |
| **Smoothed Return (20)** | אפיזודה | ממוצע נע של 20 | עולה חלק | מתיישר נמוך = נתקע באופטימום מקומי |
| **Loss / Mean \|TD Error\|** | אפיזודה | `mean(\|δ\|)` | יורד לאט | **עולה בהתמדה = divergence** |
| **Exploration ε** | אפיזודה | ε | יורד לפי הלוח | אם נשאר 1.0 — ה-decay לא עובד |
| **Steps per episode** | אפיזודה | צעדים | יורד = פתרון יעיל | קבוע על המקסימום = timeout תמיד |
| **Success rate (20)** | אפיזודה | שיעור 0–1 | עולה לכיוון 1 | נשאר 0 = לא נפתר |
| **Weight norm ‖w‖** | אפיזודה | נורמת המשקלים | מתייצב | **גדל בלי גבול = divergence בקירוב פונקציה** |
| **Largest value change** (חדר 1) | sweep | `delta`, סקאלה לוגריתמית | יורד אל קו ה-θ | לא יורד = θ קטן מדי או γ=1 |
| **Train/validation/test** (חדר 5) | אפיזודה | שיעור בריחה | שלושת הקווים עולים יחד | **train טוב ו-test גרוע = שינון (overfitting)** |

### למה Reward ו-TD error לא חייבים להשתפר מונוטונית

1. **חקירה**: כל עוד `ε > 0` הסוכן לוקח פעולות מקריות בכוונה. אפיזודה עם צעד מקרי גרוע תיתן Return נמוך גם אם המדיניות טובה.
2. **סביבה סטוכסטית**: בחדר 2 הגשר קורס לפי הגרלה; באותה מדיניות בדיוק אפשר לחצות בהצלחה או ליפול.
3. **חדר 5**: ה-layout שונה בכל אפיזודה. מפה קשה תיתן Return נמוך גם ממדיניות טובה.
4. **TD error** גדל כשהסוכן מגלה אזור חדש — זה סימן לחקירה מוצלחת, לא לכשל.

לכן **הממוצע הנע** הוא הקו שצריך להסתכל עליו, והסדרה הגולמית מוצגת מתחתיו כדי שהרעש שמוחלק יהיה גלוי ולא משוער.

### למה ε יורד בכוונה

בהתחלה הסוכן לא יודע כלום ולכן חייב לחקור. אחרי שנצבר ניסיון, חקירה היא בעיקר בזבוז — ולכן `ε` יורד. הוא **לא** יורד ל-0 אלא ל-`epsilon_min` (0.05 בברירת מחדל), כדי שהסוכן לא ייתקע במדיניות אחת לנצח.

---

## 8. המדד שמוצג כ-Loss — הגדרה מדויקת

בפרויקט **אין רשת נוירונים ואין DQN**, ולכן אין loss של רשת. המדד המוצג הוא:

```
convergence = mean(|δ_t|)   על כל צעדי האפיזודה
```

כאשר `δ` הוא ה-TD error של האלגוריתם שרץ:

* **Q-Learning:** `δ = r + γ·max_a' Q(s',a') − Q(s,a)`
* **SARSA:** `δ = r + γ·Q(s',a') − Q(s,a)`
* **Semi-gradient:** אותו `δ`, כאשר `Q` מוערך על ידי `wᵀx(s,a)`

בקוד (`game/session.py`):

```python
self.episode["errorTotal"] += abs(error)
self.episode["errorCount"] += 1
...
"convergence": (episode["errorTotal"] / errors) if errors else 0.0
```

זה **ממוצע**, לא סכום — ולכן אפיזודה ארוכה לא מקבלת ערך גבוה רק בגלל אורכה.

**מה זה לא:** זה לא MSE, לא Huber loss ולא DQN loss. הכותרת על המסך היא
`Loss / Mean |TD Error|` — "loss-like convergence diagnostic".

---

## 9. מבנה התיקיות

| קובץ | תפקיד |
|---|---|
| `serve.py` | שרת HTTP מספריית התקן + API ב-JSON. אין תלויות חיצוניות. |
| `game/config.py` | מפרטי הפרמטרים (טווח, ברירת מחדל, הסבר) ומראה הישויות. |
| `game/rooms.py` | הגדרת חמשת החדרים: מפות, תגמולים, פרמטרים, גרפים וטקסט התדריך. |
| `game/grid.py` | סביבת הלוח הטבלאית (חדרים 1–3): אריחים, מעברים, החלקה, קריסה. |
| `game/reactor.py` | הגנרטורים, המפתחות, הדלתות והשומר של חדר 3. |
| `game/drone.py` | הפיזיקה הרציפה של חדר 4. |
| `game/warehouse.py` | המחסן הפרוצדורלי, החישה והתצפית של חדר 5. |
| `game/session.py` | לולאת האימון, הערכה עם משקלים קפואים, ורישום ה-history. |
| `game/recorder.py` | הקלטת אפיזודות פריים-אחר-פריים ל-Replay. |
| `game/definition.py` | תרגום חדר לחוזה שהממשק מצפה לו. שכבת תצוגה בלבד. |
| `game/comparison.py` | הרצת כמה שיטות על אותה מפה ואותו seed. |
| `game/algorithms/value_iteration.py` | Value Iteration — סריקות עד `delta < theta`. |
| `game/algorithms/policy_iteration.py` | Policy Iteration — להשוואה בחדר 1. |
| `game/algorithms/sarsa.py` | SARSA — on-policy. |
| `game/algorithms/expected_sarsa.py` | Expected SARSA. |
| `game/algorithms/q_learning.py` | Q-Learning — off-policy. |
| `game/algorithms/double_q_learning.py` | Double Q-Learning. |
| `game/algorithms/semi_gradient_sarsa.py` | Semi-gradient SARSA (חדר 4). |
| `game/algorithms/semi_gradient_q.py` | Semi-gradient Q-Learning (חדר 5). |
| `game/algorithms/discretised_q.py` | טבלה על מצב מעוגל — להשוואה בחדר 4. |
| `game/algorithms/tile_coding.py` | Tile Coding — הפריסה לאריחים. |
| `game/algorithms/linear.py` | הקירוב הליניארי `wᵀx`. |
| `game/algorithms/tabular.py`, `base.py` | תשתית משותפת (ε-greedy, לוח ה-decay). |
| `web/room/index.html` | מסך החדר. |
| `web/assets/room/shell.js` | הבקר של מסך החדר: כרטיסים, ייצוא, פאנלים. |
| `web/assets/room/charts.js` | כל הגרפים. הממוצע הנע מחושב על הסדרה המלאה. |
| `web/assets/room/playback.js` | מכונת המצבים של ה-Replay. |
| `web/assets/room/renderer.js` | ציור העולם. |
| `web/assets/room/compare.js` | השוואת שיטות. |
| `web/assets/room/producer.js` | התקשורת עם השרת. |
| `web/assets/room/contract.js` | חוזה הנתונים, מתועד. |
| `tests/` | חמישה קבצי pytest, 300 בדיקות. |
| `README.md` | תיעוד הפרויקט באנגלית. |

---

## 10. שאלות שסביר שיישאלו

**למה Tile Coding ולא DQN?**
Tile Coding עם קירוב ליניארי הוא Function Approximation תקין לחלוטין. הוא פותר בדיוק את הבעיה שהחדר מציג — מרחב מצבים רציף שאין לו טבלה — והוא שקוף: אפשר להראות איזה אריח נדלק ואיזה משקל עלה. אין בפרויקט רשת נוירונים, ולכן גם אין loss של רשת.

**איך יודעים שהלמידה עובדת ולא סתם מקריות?**
ארבעה סימנים ביחד: ה-Return המוחלק עולה, שיעור ההצלחה עולה, `mean(|δ|)` יורד, ומספר הצעדים יורד. ובחדר 5 יש מדד חזק יותר — שיעור הבריחה על מפות שלא אומנו עליהן.

**למה בחדר 2 המדיניות עוברת למסלול הארוך?**
כי נפילה מפסידה גם את הפרס וגם משלמת את הענישה. הגשר משתלם רק כל עוד
`crossing risk < steps saved / (exit + fall + steps walked in)`. עם `+38/-15` הסף הוא בסביבות 25%, ולכן מתחת ל-0.20 נבחר הגשר ומעל 0.30 המסלול הבטוח.

**מה ההבדל בין SARSA ל-Q-Learning כאן?**
SARSA הוא on-policy: הוא מעריך את המסלול שהוא **באמת** הולך בו, כולל צעדי חקירה מקריים ליד פיר קטלני. Q-Learning מעריך מסלול מושלם. לכן SARSA זונח את הגשר מוקדם יותר.
