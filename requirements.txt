# The application itself needs nothing: `python3 serve.py` runs on the standard
# library alone, and the browser half is hand-written JavaScript with no build
# step. This file exists only for the test run.
pytest>=8.0
